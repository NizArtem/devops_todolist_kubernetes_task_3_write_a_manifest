apiVersion: v1
kind: Pod
metadata:
  name: busybox-test
  namespace: todoapp
spec:
  containers:
  - name: ikulyk404/busyboxplus:curl
    image: ikulyk404/busyboxplus:curl
    command: ["sleep", "3600"]
  restartPolicy: Never
